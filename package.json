{
  "name": "nobateno-panels",
  "version": "1.0.0",
  "license": "MIT",
  "packageManager": "pnpm@11.6.0",
  "engines": {
    "node": ">=22.22.2"
  },
  "scripts": {
    "nx": "nx",
    "customer": "nx run customer-panel:dev",
    "provider": "nx run provider-panel:dev",
    "build": "nx run-many -t build",
    "lint": "nx run-many -t lint",
    "typecheck": "nx run provider-panel:typecheck",
    "test": "nx run provider-panel:test",
    "deploy": "opennextjs-cloudflare build && opennextjs-cloudflare deploy"
  },
  "private": true,
  "dependencies": {},
  "devDependencies": {
    "@nx/js": "23.1.1",
    "@nx/workspace": "23.1.1",
    "nx": "23.1.1"
  }
}
